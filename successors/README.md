# Generated successors

The original C# `FirstNeuralNetwork` experiment remains unchanged at the repository root.

## Adaptive Connectivity Diagnostic Network — C++

`adaptive-connectivity-diagnostic-network-cpp.zip.b64` is the exact Base64 transport of the generated ACDN C++ package. It is preserved here as a successor experiment rather than silently replacing the original implementation.

Archive SHA-256 after Base64 decoding:

`f1146e15b4ca8e0bc6060d19fe219876f883f223898f64279c46c538145905e7`

Restore on GNU/Linux:

```bash
base64 -d successors/adaptive-connectivity-diagnostic-network-cpp.zip.b64 > adaptive-connectivity-diagnostic-network-cpp.zip
sha256sum adaptive-connectivity-diagnostic-network-cpp.zip
unzip adaptive-connectivity-diagnostic-network-cpp.zip
```

The generated package is a dependency-free C++20 implementation with CMake/CTest, a deterministic synthetic demo, executable tests, sanitizer support, cross-platform CI configuration, architecture/concept/experiment documentation, and validation artifacts.

Its project boundary is explicit: it investigates sparse modular recurrent groups and controlled connection-on/connection-off structural adaptation. It does not claim biological realism, superiority to conventional recurrent networks, complete causal identification, or production readiness.
